github-rank {
  github {
    git-hub-graphql-uri = "https://api.github.com/graphql"
    git-hub-auth-token = "super-fake-token"
  }
  cache {
    capacity = 100
    capacity = ${?CACHE_CAPACITY}
    ttl = 1 minute
    ttl = ${?CACHE_TIME_TO_LIVE}
  }
}